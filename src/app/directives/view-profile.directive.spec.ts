import { ViewProfileDirective } from './view-profile.directive';

describe('ViewProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewProfileDirective();
    expect(directive).toBeTruthy();
  });
});
